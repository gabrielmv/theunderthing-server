server {
    listen      80;

    server_name plex.dinaren.ml;
    return 301 https://plex.dinaren.ml:443$request_uri;
}

server {
    listen        443 ssl;
    server_name   plex.dinaren.ml;

    ssl_certificate /certs/config/live/dinaren.ml/cert.pem;
    ssl_certificate_key /certs/config/live/dinaren.ml/privkey.pem;

    location / {
        proxy_pass http://10.0.0.101:32400;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
    }
}

server {
    listen      80;

    server_name plex.dinaren.tk;
    return 301 https://plex.dinaren.tk:443$request_uri;
}

server {
    listen        443 ssl;
    server_name   plex.dinaren.tk;

    ssl_certificate /certs/config/live/dinaren.tk/cert.pem;
    ssl_certificate_key /certs/config/live/dinaren.tk/privkey.pem;

    location / {
        proxy_pass http://10.0.0.101:32400;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
    }
}

