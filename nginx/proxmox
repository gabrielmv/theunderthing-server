server {
    listen      80;

    server_name proxmox.dinaren.ml;
    return 301 https://proxmox.dinaren.ml:443$request_uri;
}

server {
    listen        443 ssl;
    server_name   proxmox.dinaren.ml;

    ssl_certificate /certs/config/live/dinaren.ml/cert.pem;
    ssl_certificate_key /certs/config/live/dinaren.ml/privkey.pem;

    location / {
        proxy_pass https://10.0.0.105:8006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
    }
}

server {
    listen      80;

    server_name proxmox.dinaren.tk;
    return 301 https://proxmox.dinaren.tk:443$request_uri;
}

server {
    listen        443 ssl;
    server_name   proxmox.dinaren.tk;

    ssl_certificate /certs/config/live/dinaren.tk/cert.pem;
    ssl_certificate_key /certs/config/live/dinaren.tk/privkey.pem;

    location / {
        proxy_pass https://10.0.0.105:8006;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;
    }
}
